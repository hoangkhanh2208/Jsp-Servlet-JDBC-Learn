package com.hoangkhanh.beans;

public class Authenticator {
	public static boolean authenticator(UserAccount user) {
		if("admin".equals(user.getUserName()) && "123".equals(user.getPassword())) {
			return true;
		}else {
			return false;
		}
	}
}
